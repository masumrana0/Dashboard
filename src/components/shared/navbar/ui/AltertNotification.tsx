import { INotificationMessage } from '@/interface/message';
import React from 'react';

const AltertNotification:React.FC<{notification:INotificationMessage}> = ({notification}) => {
    return (
        <div>
            
        </div>
    );
};

export default AltertNotification;